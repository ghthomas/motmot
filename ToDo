TO DO:

Priority
- Add option to fit lambda simultaneously with other trait evolution models.
- Add ACDC (EB) and time slice models
- Vectorize for loops in transformPhylo (OU, delta, time slice)
- Function for simple profile plots for single parameter models, possibly contour plots for multiparameter models?
- Allow results to be reported even if CIs fail
- Allow user to change options in the optimiser. 
- Set default to try multiple starting points for OU model (other models?).
 
Extensions
- MCMC estimation for ML.RatePhylo (may be useful for small trees?)